// Category index: ArchiveCard grid; category detail: BlogList.
import '../styles/archive.css';
import '../styles/blog-ui.css';

export default function CategoriesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
