// Series index: ArchiveCard grid; series detail: archive-list.
import '../styles/archive.css';

export default function SeriesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
