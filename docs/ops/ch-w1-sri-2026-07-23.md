# CH-W1 SRI 回归证据 · 2026-07-23

> Worktree: `ch-w1-sri` · Branch: `xvyimu/ch-w1-sri` · **local only（未 push）**

## 环境

| Item                    | Value                                                                   |
| ----------------------- | ----------------------------------------------------------------------- |
| Worktree path           | `C:\Users\yuanjia\orca\workspaces\Chronicle\ch-w1-sri`                  |
| Base tip at start       | `f60858e` (`docs: fix CoC channel, changelog honesty, CI doc coverage`) |
| Node                    | v24.16.0（engines 声明 22.x；仅 engine warn，门闩可跑）                 |
| pnpm                    | 11.8.0                                                                  |
| Package manager install | `pnpm install --frozen-lockfile` → **exit 0**                           |

## 命令与 exit code

| #   | Command                                                                                  | Exit      | Notes                                                            |
| --- | ---------------------------------------------------------------------------------------- | --------- | ---------------------------------------------------------------- |
| 1   | `pnpm install --frozen-lockfile`                                                         | **0**     | 首次 worktree 装依赖                                             |
| 2   | `pnpm test:sri`                                                                          | **0**     | 6/6 pass                                                         |
| 3   | `pnpm check:sri-smoke`                                                                   | **0**     | offline：gate + skip `.next`                                     |
| 4   | `pnpm audit --registry=https://registry.npmjs.org --audit-level=high`                    | **1 → 0** | 初跑 4× high on `next@16.2.9`；修后 0 known vulns                |
| 5   | `pnpm exec cross-env NEXT_PUBLIC_SITE_URL=https://incca.ccwu.cc ENABLE_SRI=1 pnpm build` | **0**     | Next 16.2.11 · experiments.sri on                                |
| 6   | `pnpm check:sri -- --file .next/server/pages/500.html --expect on`                       | **0**     | `total=8 protected=6 unprotected=2` · PASS（mixed 符合脚本语义） |
| 7   | `pnpm exec cross-env ENABLE_SRI=1 pnpm check:sri-smoke -- --require-build`               | **0**     | `integrityHits=14` / 2 html                                      |

## 红 → 最小修复

**根因：** CI 对齐的 `pnpm audit --audit-level=high` 在 `next@16.2.9` 上报 4 high（GHSA-6gpp-xcg3-4w24 / GHSA-m99w-x7hq-7vfj / GHSA-89xv-2m56-2m9x / GHSA-p9j2-gv94-2wf4）；补丁线 `>=16.2.11`。

**Diff（最小）：**

- `next` `16.2.9` → `16.2.11`（dependencies）
- `eslint-config-next` `16.2.9` → `16.2.11`
- `@next/bundle-analyzer` `^16.2.9` → `^16.2.11`
- `pnpm-lock.yaml` 同步
- `next.config.ts` 注释版本号对齐（行为不变）

**未做：** Dependabot 大版本扫荡、CSP 放宽、`ENABLE_SRI` 生产 flip、视觉/MDX 改动、`git push`。

## SRI 结论

| 维度       | 结果                                                                         |
| ---------- | ---------------------------------------------------------------------------- |
| 配置门     | `ENABLE_SRI === '1'` + `algorithm: 'sha384'` + `sriExperiment` spread — PASS |
| 单元       | `scripts/check-sri.test.mjs` 6 pass                                          |
| 产物       | `ENABLE_SRI=1` build 后 500.html 含 sha384 integrity（≥1 即可 expect on）    |
| audit high | 修后 **0** known vulnerabilities                                             |

**总评：绿 · 可 review（local commit only）**

## 复现

```bash
pnpm install --frozen-lockfile
pnpm test:sri
pnpm check:sri-smoke
pnpm audit --registry=https://registry.npmjs.org --audit-level=high
pnpm exec cross-env NEXT_PUBLIC_SITE_URL=https://incca.ccwu.cc ENABLE_SRI=1 pnpm build
pnpm check:sri -- --file .next/server/pages/500.html --expect on
pnpm exec cross-env ENABLE_SRI=1 pnpm check:sri-smoke -- --require-build
```

## Git

- 变更已 **local commit**（见本分支 tip）
- **未** `git push`
